import { FileDataInterceptor } from '../../src/files/file-data.interceptor';

describe('FileDataInterceptor', () => {
  it('should be defined', () => {
    expect(new FileDataInterceptor()).toBeDefined();
  });
});
